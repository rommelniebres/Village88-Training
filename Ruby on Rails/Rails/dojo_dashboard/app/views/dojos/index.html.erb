<!DOCTYPE html>
<html>

<head>
    <style>
        #dojos {
            font-family: Arial, Helvetica, sans-serif;
            border-collapse: collapse;
            width: 100%;
        }

        #dojos td,
        #dojos th {
            border: 1px solid #ddd;
            padding: 8px;
        }

        #dojos tr:nth-child(even) {
            background-color: #f2f2f2;
        }

        #dojos tr:hover {
            background-color: #ddd;
        }

        #dojos th {
            padding-top: 12px;
            padding-bottom: 12px;
            text-align: left;
            background-color: #04AA6D;
            color: white;
        }

        #new_dojo {
            display: block;
            margin-top: 20px;
        }

        input[type=submit] {
            background: none !important;
            border: none;
            padding: 0 !important;
            font-size: 16px;
            color: red;
            text-decoration: underline;
            cursor: pointer;
        }
    </style>
</head>

<body>
    <h2>Listing <%= @dojos.count %> Dojo(s)
    </h2>
    <table id="dojos">
        <tr>
            <th>Coding Dojo Branch</th>
            <th>Street</th>
            <th>City</th>
            <th>State</th>
            <th colspan="3">Action</th>
        </tr>
        <% @dojos.each do |dojo| %>
            <tr>
                <td>
                    <%= dojo.branch %>
                </td>
                <td>
                    <%= dojo.street %>
                </td>
                <td>
                    <%= dojo.city %>
                </td>
                <td>
                    <%= dojo.state %>
                </td>
                <td>
                    <a href="/dojos/<%= dojo.id %>">Show</a>
                </td>
                <td>
                    <a href="/dojos/<%= dojo.id %>/edit">Edit</a>
                </td>
                <td>
                    <form action="/dojos/<%= dojo.id %>" method="post">
                        <input type="hidden" name="_method" value="delete">
                        <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
                        <input type="submit" value="Delete">
                    </form>
                </td>
            </tr>
            <% end %>
    </table>
    <a href="/dojos/new" id="new_dojo">New Dojo</a>
    <% flash.each do |type, msg| %>
        <div>
            <%= msg %>
        </div>
        <% end %>
</body>

</html>